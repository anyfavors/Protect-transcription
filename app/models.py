"""
Pydantic models. Currently empty — request/response shapes for the few endpoints
that need them are inlined in the route handlers. Kept as a module so future
shared types have an obvious home.
"""
